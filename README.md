# ServersMonitor

Self-hosted server monitoring hub in the spirit of Beszel, with Azure sandbox management on top
(later lots). Two Go binaries: `smhub` (hub, web UI, API) and `smagent` (metrics agent).

Status: lot 1 (core) in progress. See `docs/superpowers/specs/`.
